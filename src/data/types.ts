export type Item = {
    name: string,
    category: string,
    price: number,
    imageTag: string,
    value?: number,
    quantity?: string,
    strength?: string,
    origin?: string,
    flavor?: string[],
}